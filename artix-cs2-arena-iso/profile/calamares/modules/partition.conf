# Partition configuration — auto layout for 512GB NVMe
# EFI + Boot + XFS Root

defaultPartitionTableType: gpt
defaultFileSystemType: xfs

# Enable these partition operations
availableFileSystemTypes: [xfs, ext4, btrfs, f2fs]

# Partition suggestions shown to user
suggestedPartitionTable: gpt
